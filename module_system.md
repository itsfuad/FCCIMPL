

# First program
When running, compiler first checks its nearest config file. If not found, then it creates a default one and runs the program.
You can import builtin modules without config file. But its required when you import libraries