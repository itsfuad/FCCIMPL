// Comprehensive error type tests

// 1. Basic error type
fn divide(a: i32, b: i32) -> i32 ! str {
    if b == 0 {
        return "division by zero"!;
    }
    return a / b;
}

// 2. Error type with custom error type
fn parseNumber(s: str) -> i32 ! ParseError {
    return 0!;
}

// 3. Optional error type (optional comes first: T? ! E)
fn findUser(id: i32) -> User? ! DatabaseError {
    return user!;
}

// 4. Array with error type
fn processItems(items: []str) -> []i32 ! str {
    return result!;
}

// 5. Anonymous function with error type
const validator := fn (value: str) -> bool ! ValidationError {
    return true;
};
