// Test file to show different diagnostic colors

// This should trigger an info diagnostic (trailing comma)
type Point struct {
    .x: i32,
    .y: i32,
};

// This should trigger an error (undefined variable)
let bad := undefined_var;

// This should also show an info (trailing comma)
let p := Point{.x = 1, .y = 2,};
