// Test variadic parameter support

// Function with variadic parameter at the end
fn sum(base: i32, nums: ...i32) -> i32 {
    let total := base;
    return total;
}

// Function with variadic parameter in middle (parser should allow this, semantics will check later)
fn format(prefix: str, values: ...str, suffix: str) -> str {
    return prefix;
}

// Function with only variadic parameter
fn print(args: ...str) {
    return;
}

// Anonymous function with variadic parameter
const handler := fn (code: i32, errors: ...str) -> str {
    return "error";
};
