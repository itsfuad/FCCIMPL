// Test file showing all diagnostic severity colors

// Info diagnostic (bold cyan)
type Point struct {
    .x: i32,
    .y: i32,
};

// Error diagnostic (bold red) - undefined variable
let bad := undefined_var + 123;

// Info diagnostic (bold cyan)
let p := Point{.x = 1, .y = 2,};
