// Comprehensive test showing different error scenarios

// Test 1: Same-line labels (compact format)
fn test1() {
    const XXX: i32 = "str"; 
    XXX = 20;
}

// Test 2: Different-line labels (routed format)
const GLOBAL: f64 = 3.14;

fn test2() {
    GLOBAL = 2.71;
}

// Test 3: Type mismatch (compact format)
fn test3() {
    let x: i32 = 42;
    let y: str = x;
}
