// Test all interface type variations

// Test 1: Standalone interface (any type)
fn printAny(arg: interface{}) {
    let msg := "printing any";
}

// Test 2: Empty interface with braces (same as standalone)
fn printAny2(arg: interface{}) {
    let msg := "printing any";
}

// Test 3: Interface with methods
type Stringer interface {
    toString() -> str;
};

fn printStringer(s: Stringer) {
    let msg := "printing stringer";
}

// Test 4: Multiple interface parameters
fn compare(a: interface{}, b: interface{}) -> bool {
    return true;
}

// Test 5: Interface as return type
fn getAny() -> interface{} {
    let x := 42;
    return x;
}

// Test 6: Interface in struct
type Container struct {
    .value: interface{},
};

// Test 7: Array of interface
let items: []interface{} = [1, 2, 3];

// Test 8: Map with interface values
let m: map[str]interface{} = map[str]interface{}{
    "key1" => "value1",
    "key2" => "value2",
};
