// Test elvis operator error cases

// Test 1: Type mismatch - wrong default type
let opt1: i32? = 10;
let bad1: i32 = opt1 ?: "wrong type";  // ERROR: str not compatible with i32

// Test 2: Elvis with non-optional (should warn)
let x: i32 = 5;
let result: i32 = x ?: 0;  // WARNING: elvis typically used with optional types

// Test 3: Assigning elvis result with wrong type
let opt2: str? = "hello";
let bad2: i32 = opt2 ?: "default";  // ERROR: str not assignable to i32
